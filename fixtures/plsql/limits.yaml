# 走査行数の上限（2026-09-17 の決定）。
#
# 生成コードは cursor の行を先に全部読むので、動く行数はメモリで決まる。上限は業務ごとに違うため、
# 既定をここに置き、routine 単位で上書きする。超えたら例外で止まる。
#
# **この既定値に根拠は無い。** 各 routine について「何行まで来うるか」を業務側と決めたら、
# routines に書いて根拠をコメントに残すこと。
scanRows:
  default: 10000
  routines:
    # 受注明細。1 注文あたりの明細数なので小さい
    pkg_order_pricing.order_total: 1000
