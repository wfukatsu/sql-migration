{
  "pattern": "P1+P6",
  "source_dialect": "oracle",
  "source_sql": "-- G-3. 再帰 WITH（標準SQL、他DBへ移行しやすい書き方）\nWITH org (employee_id, last_name, manager_id, lvl, path) AS (\n  SELECT employee_id, last_name, manager_id, 1, CAST(last_name AS VARCHAR2(4000))\n  FROM   employees WHERE manager_id IS NULL\n  UNION ALL\n  SELECT e.employee_id, e.last_name, e.manager_id, o.lvl + 1, o.path || '/' || e.last_name\n  FROM   employees e JOIN org o ON e.manager_id = o.employee_id\n)\nSEARCH DEPTH FIRST BY last_name SET ord\nSELECT lvl, LPAD(' ', 2 * (lvl - 1)) || last_name AS org_chart, path\nFROM   org\nORDER  BY ord",
  "fetch": [
    {
      "table": "employees",
      "namespace": "hr",
      "alias": "employees",
      "columns": [
        "employee_id",
        "last_name",
        "manager_id"
      ],
      "column_types": {
        "employee_id": "INT",
        "first_name": "TEXT",
        "last_name": "TEXT",
        "email": "TEXT",
        "hire_date": "DATE",
        "job_id": "TEXT",
        "salary": "DOUBLE",
        "commission_pct": "DOUBLE",
        "manager_id": "INT",
        "department_id": "INT"
      },
      "predicates": [],
      "scalardb_sql": "SELECT employee_id, last_name, manager_id FROM hr.employees",
      "access_path": "CROSS_PARTITION",
      "max_rows": 10000,
      "index_columns": [
        [
          "employee_id"
        ],
        [
          "manager_id"
        ]
      ],
      "residual_types": {}
    }
  ],
  "residual": {
    "java": {
      "engine": "h2",
      "mode": "Oracle",
      "sql": "/* G-3. 再帰 WITH（標準SQL、他DBへ移行しやすい書き方） */ WITH RECURSIVE org(employee_id, last_name, manager_id, lvl, path) AS (SELECT employee_id, last_name, manager_id, 1, CAST(last_name AS VARCHAR2(4000)) FROM employees WHERE manager_id IS NULL UNION ALL SELECT e.employee_id, e.last_name, e.manager_id, o.lvl + 1, o.path || '/' || e.last_name FROM employees e JOIN org o ON e.manager_id = o.employee_id) SEARCH DEPTH FIRST BY last_name SET ord SELECT lvl, LPAD(' ', 2 * (lvl - 1)) || last_name AS org_chart, path FROM org ORDER BY ord NULLS LAST",
      "build_indexes": false
    },
    "python": {
      "engine": "sqlite3",
      "sql": "/* G-3. 再帰 WITH（標準SQL、他DBへ移行しやすい書き方） */ WITH org(employee_id, last_name, manager_id, lvl, path) AS (SELECT employee_id, last_name, manager_id, 1, CAST(last_name AS TEXT(4000)) FROM employees WHERE manager_id IS NULL UNION ALL SELECT e.employee_id, e.last_name, e.manager_id, o.lvl + 1, o.path || '/' || e.last_name FROM employees AS e JOIN org AS o ON e.manager_id = o.employee_id) SEARCH DEPTH FIRST BY last_name SET ord SELECT lvl, LPAD(' ', 2 * (lvl - 1)) || last_name AS org_chart, path FROM org ORDER BY ord NULLS LAST"
    }
  },
  "write": null,
  "guardrails": {
    "requires_cross_partition_scan": true,
    "row_limit": 10000
  },
  "unresolved": [],
  "notes": [
    "employees: pushdown manager_id IS NULL",
    "employees: referenced by several scopes with different predicates; fetching without predicate",
    "java: Oracle semantics made explicit for H2 (date functions, NULL ordering)",
    "java: WITH RECURSIVE spelled out for H2"
  ],
  "transaction": {
    "read_only": true
  },
  "recommended_config": {
    "transaction": "read-only (DistributedTransactionManager.beginReadOnly / SqlSession.beginReadOnly, 3.16+)",
    "scalar.db.scan_fetch_size": 1000,
    "scalar.db.cluster.client.scan_fetch_size": 1000,
    "scalar.db.cross_partition_scan.enabled": true,
    "isolation_note": "SERIALIZABLE re-executes every scan at commit; SNAPSHOT or READ_COMMITTED avoids it but applies to the whole node"
  }
}