{
  "pattern": "P1+P6",
  "source_dialect": "oracle",
  "source_sql": "--==============================================================================\n-- G. WITH 句・階層問合せ\n--==============================================================================\n-- G-1. 副問合せのファクタリング\nWITH dept_stats AS (\n  SELECT department_id, AVG(salary) AS avg_sal\n  FROM   employees GROUP BY department_id\n)\nSELECT e.last_name, e.salary, ROUND(s.avg_sal) AS dept_avg\nFROM   employees e JOIN dept_stats s ON s.department_id = e.department_id\nWHERE  e.salary > s.avg_sal",
  "fetch": [
    {
      "table": "employees",
      "namespace": "hr",
      "alias": "e",
      "columns": [
        "employee_id",
        "last_name",
        "salary",
        "department_id"
      ],
      "column_types": {
        "employee_id": "INT",
        "first_name": "TEXT",
        "last_name": "TEXT",
        "email": "TEXT",
        "hire_date": "DATE",
        "job_id": "TEXT",
        "salary": "DOUBLE",
        "commission_pct": "DOUBLE",
        "manager_id": "INT",
        "department_id": "INT"
      },
      "predicates": [],
      "scalardb_sql": "SELECT employee_id, last_name, salary, department_id FROM hr.employees",
      "access_path": "CROSS_PARTITION",
      "max_rows": 10000,
      "index_columns": [
        [
          "employee_id"
        ],
        [
          "department_id"
        ]
      ],
      "residual_types": {}
    }
  ],
  "residual": {
    "java": {
      "engine": "h2",
      "mode": "Oracle",
      "sql": "--==============================================================================\n-- G. WITH 句・階層問合せ\n--==============================================================================\n-- G-1. 副問合せのファクタリング\nWITH dept_stats AS (\n  SELECT department_id, AVG(salary) AS avg_sal\n  FROM   employees GROUP BY department_id\n)\nSELECT e.last_name, e.salary, ROUND(s.avg_sal) AS dept_avg\nFROM   employees e JOIN dept_stats s ON s.department_id = e.department_id\nWHERE  e.salary > s.avg_sal",
      "build_indexes": false
    },
    "python": {
      "engine": "sqlite3",
      "sql": "/* ============================================================================== */ /* G. WITH 句・階層問合せ */ /* ============================================================================== */ /* G-1. 副問合せのファクタリング */ WITH dept_stats AS (SELECT department_id, AVG(salary) AS avg_sal FROM employees GROUP BY department_id) SELECT e.last_name, e.salary, ROUND(s.avg_sal) AS dept_avg FROM employees AS e JOIN dept_stats AS s ON s.department_id = e.department_id WHERE e.salary > s.avg_sal"
    }
  },
  "write": null,
  "guardrails": {
    "requires_cross_partition_scan": true,
    "row_limit": 10000
  },
  "unresolved": [],
  "notes": [],
  "transaction": {
    "read_only": true
  },
  "recommended_config": {
    "transaction": "read-only (DistributedTransactionManager.beginReadOnly / SqlSession.beginReadOnly, 3.16+)",
    "scalar.db.scan_fetch_size": 1000,
    "scalar.db.cluster.client.scan_fetch_size": 1000,
    "scalar.db.cross_partition_scan.enabled": true,
    "isolation_note": "SERIALIZABLE re-executes every scan at commit; SNAPSHOT or READ_COMMITTED avoids it but applies to the whole node"
  }
}