# 生成コードの外で決めること — 確認一覧

出た項目 3 / 28　未決 0　決定 3　対象外 25

## OPS — 運用で決めること

| ID | 項目 | 状態 | 決定・案 | 出た根拠 |
|---|---|---|---|---|

## CALL — 呼び出し側で決めること

| ID | 項目 | 状態 | 決定・案 | 出た根拠 |
|---|---|---|---|---|
| CALL-5 | 楽観制御で弾かれたときの再試行 | 決定 | 衝突だけを最大 3 回、50 ms から倍々の指数バックオフで再試行する。-20103（ポイント不足）と -20102 は再試行しない。use_points に採番も外部通知も無く、履歴の番号は読み直されるので、再試行は二重実行にならない（移行責任者、2026-09-20） | 診断 `OPTIMISTIC` / `RMW_SPLIT` / `MERGE_SPLIT`（楽観制御へ移したもの） — pkg_points.use_points: OPTIMISTIC |

## BIZ — 業務ロジックとの整合

| ID | 項目 | 状態 | 決定・案 | 出た根拠 |
|---|---|---|---|---|
| BIZ-4 | 行ロックが無くなり、衝突が分かるタイミングが変わる | 決定 | 受け入れる。同じ会員の同時利用は、待たされる代わりに、後から commit したほうが弾かれて再試行になる。残高がマイナスにならないことは保たれる。現行は NOWAIT を使っておらず、待たずに諦めることに業務上の意味は無い（移行責任者、2026-09-20） | 診断 `OPTIMISTIC` / `RMW_SPLIT` / `MERGE_SPLIT`（楽観制御へ移したもの） — pkg_points.use_points: OPTIMISTIC |
| BIZ-5 | MERGE を「読んでから選ぶ」に割った | 決定 | 該当しない。pkg_points に MERGE は無く、この項目は OPTIMISTIC の診断（use_points の楽観制御）と一緒に出ただけである。確かめること無し（移行責任者、2026-09-20） | 診断 `OPTIMISTIC` / `RMW_SPLIT` / `MERGE_SPLIT`（楽観制御へ移したもの） — pkg_points.use_points: OPTIMISTIC |
