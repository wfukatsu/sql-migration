"""Migration Explorer: one read-only HTML page for the investigation that comes before a migration.

Which PL/SQL and SQL touch which table, what that table is tied to, and how much of it there is -- from the
analysis output (`python -m plsql.cli`), the application's SQL files, and a catalog snapshot of the database.
See docs/guide/explorer.md.
"""
